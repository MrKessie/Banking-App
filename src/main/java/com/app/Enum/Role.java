package com.app.Enum;

public enum Role {
    ROLE_ADMIN, //Administrator with elevated privileges
    ROLE_TELLER, //Bank teller who processes transactions
    ROLE_CLIENT //A normal user with basic account access
}
